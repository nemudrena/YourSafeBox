<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.myapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ActivityAroma"></activity>
        <activity android:name=".ActivitySence" />
        <activity android:name=".ActivityTaste" />
        <activity android:name=".ActivityPlaces" />
        <activity android:name=".ActivityMovies"
            android:launchMode="singleTask"/>
        <activity android:name=".ActivityMusic" />
        <activity android:name=".ActivityPics" />
        <activity android:name=".ActivityWays" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>